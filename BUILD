for the lib, just do a 'make'.  you can strip it and install it
as you wish.

for the deb, do whatever debian voodoo works for you.

a quick unsigned deb can be done with:

fakeroot debian/build binary
